# oregon-smb-directory

> **Editing anything on the site** (a listing, verification, rating, city, industry, service
> category, blog, report, Best Of list, or config)? Read
> **[`DIRECTORY-EDITING-AND-CONFIGURATION-GUIDE.md`](DIRECTORY-EDITING-AND-CONFIGURATION-GUIDE.md)**
> first. It names the exact file for each change and includes the step-by-step
> new-listing checklist (section 2.1).

Agent contract. Structure is generated by `projectstd`; prose is not.

## Where the intel lives

<!-- projectstd:begin id=pointer v=1 -->
**How to navigate this repo**

1. **Follow the pointers.** `/home/mikes/.claude/ROOT-POINTER.md` → taxonomy → store index → the unit.
2. **Progressive disclosure.** Index before file, section before document, symbol before source.
3. **Context comes from the CLIs.** `doc-intel section <title> --body` returns a section; `intel show <symbol> --body` returns a definition. Invocations: `/home/mikes/.claude/OVERLAY-WSL.md`.
4. **Locate across projects with Manticore.** `uv run --no-project --python 3.13 python /home/mikes/house-index/house.py search "<terms>" --dir <path>` (`files "<terms>"` for one row per file). Connection refused → start steps in the `house-search` skill. What *happened* in a past session is the `agent-history` skill, not Manticore.
5. **`ROADMAP.md` is not canonical.** `rmap` is: `rmap status` for the company, `rmap status --project <dir>` for one project.
6. **grep is a temporary fallback.** Use `command grep` — bare `grep` here is `ugrep --ignore-files` and returns a silent null for gitignored paths.

| store | index | what it answers |
|---|---|---|
| **root** | `/home/mikes/.claude/ROOT-POINTER.md` | which store to ask, and how the chain works |
| corpusidx | `/home/mikes/corpusidx/INDEX.md` | see ROOT-POINTER |
| doc-intel | `/home/mikes/doc-intel/indexes` | see ROOT-POINTER |
| session-audit | `/home/mikes/session-audit/INDEX.md` | see ROOT-POINTER |
| codebase-intel | `/home/mikes/codebase-intel-toolkit/INDEX.md` | see ROOT-POINTER |

**Structure index:** `/home/mikes/codebase-intel-toolkit/indexes/oregon-smb-directory`
<!-- projectstd:end id=pointer -->

## CLI toolkit

<!-- projectstd:begin id=toolkit v=1 -->
**Taxonomy — one word per store. Full version: `/home/mikes/.claude/HOUSE-TOOLKIT-TAXONOMY.md`.**

| word | system | holds |
|---|---|---|
| **CONTENT** | `corpusidx` | words — docs, transcripts, pages |
| **CODE** | `codebase-intel-toolkit` | structure — symbols, files, deps |
| **DOCS** | `doc-intel` | sections — one document set, queried by title |
| **CONDUCT** | `session-audit` | what agents did — findings, lessons |
| **WORK** | `roadmap` | planned, in progress, done |
| **SCRATCH** | `context-mode` | ephemeral sandbox, nothing durable |
| **LAYOUT** | `projectstd` | the shape every project root must have |

The same stores answer the same questions in every project.

| when you need | run |
|---|---|
| What is in the corpus? | `uv run --project /home/mikes/corpusidx corpusidx query search "<question>"` |
| Where does this symbol live? | `uv run --project /home/mikes/codebase-intel-toolkit intel show <symbol> --body` |
| How is this codebase laid out? | `uv run --project /home/mikes/codebase-intel-toolkit intel map <dir>` |
| What does this document say? | `uv run --project /home/mikes/doc-intel doc-intel query '<terms>' --db <db>` |
| What went wrong in past sessions? | `uv run --project /home/mikes/session-audit sessaudit query findings --rule <RULE>` |
| What should I work on? | `uv run --project /home/mikes/roadmap rmap next` |
| Analyse a big output without reading it | context-mode MCP — `ctx_execute`, `ctx_batch_execute`, `ctx_search` |

Python runs through `uv`, never a bare interpreter: `uv run --project <dir>` for a project with a `pyproject.toml`, `uv run --no-project python <file.py>` for a loose script.

**Where output goes:** store output stays in the store (`corpusidx/data`, `codebase-intel-toolkit/indexes/<project>`, `session-audit/data`); this project's own output stays here, in `index/` and `dist/`. A tool never writes its index into the tree it indexed.
<!-- projectstd:end id=toolkit -->

## Layout

<!-- projectstd:begin id=layout v=1 -->
```
/home/mikes/oregon-smb-directory  (node)
  _archive/
  dist/      built output — regenerated, not edited
  docs/      written documentation
  handoffs/
  public/
  rar/
  reports/   generated reports
  requests/
  scripts/   one-off and operational scripts
  src/       source

Entry points: `src/components/`, `src/content/`, `src/data/`, `src/hooks/`
```
<!-- projectstd:end id=layout -->

## Run and verify

<!-- projectstd:begin id=run v=1 -->
Run it:

- dev — `cd /home/mikes/oregon-smb-directory && npm run dev`
- start — `cd /home/mikes/oregon-smb-directory && npm run start`
- build — `cd /home/mikes/oregon-smb-directory && npm run build`
- preview — `cd /home/mikes/oregon-smb-directory && npm run preview`
<!-- projectstd:end id=run -->

## Rules

<!-- Yours. Project invariants, do-nots, and anything that cost time to learn.
     projectstd never edits below this line. -->

- Read before you write: the installed artifact is the contract, not the docs.
- Archive, never delete. Entries go in `_archive/<name>-<YYYYMMDD-HHMMSS>/`, with a
  `README.md` explaining what was removed and how to restore it.

### Canonical repo

- This repo is the live source for oregonsmbdirectory.com.
  - Branch: `master`.
  - Remote: `github.com/mikeschlottig/oregon-smb-dir-production`.
  - Worker: `oregonsmbdirectory-site`.
- **Don't work in** `/mnt/c/Users/mikes/client-and-product-apps/oregon-smb-dir-production-dev`.
  It's an older lineage. Its `HANDOFF.md` calls itself the "Canonical Worktree". That was
  true on 2026-08-18 and isn't any more.

### Where to pick up

- Read the **newest** file in `handoffs/` (`ls -t handoffs | head -1`). It is the current
  state; older handoffs are history.
- Never edit an old handoff. At session end, write a new one:
  `handoffs/<YYYY-MM-DDTHHMM>_<agent>_<topic>.md`.
- Long-running plans live in `tasks.md`, and their evidence lives in `hypothesis.md`.

### Build and deploy — use the scripts, not `npm run build`

- **Node:** `.nvmrc` pins it, and the system Node fails the build. The scripts load nvm
  themselves.
- **Build:** `bash scripts/build-bg.sh build`. Success prints `BUILD_EXIT=0`. It takes
  about 3 minutes.
- **Deploy:** `bash scripts/deploy-bg.sh "<message>"`. Success prints `DEPLOY_EXIT=0`.
- **Check rendered listings:** `bash scripts/check-listing-pages.sh <city>/<industry>/<slug> …`
  (no `city/` prefix).
- **How to wait:** run the build or deploy as one background command and wait for it to
  exit. Don't poll with sleep and grep. From a session that may end first, detach with
  `setsid nohup ... > <log> 2>&1 < /dev/null &`.

### Listings and ratings

- **Listings:** each city-and-industry pair is a shard in `src/data/businesses/<city>__<industry>.json`.
- **Adding or patching listings:** batches go in `requests/listings/` and are applied with
  `node scripts/add-listings.mjs <batch>`.
- **Ratings:** the site publishes a rating only when both of these hold:
  - the record's `googleUrl` carries the `0x…:0x…` feature ID
  - `src/data/rating-evidence.json` has a matching `"rating|reviews"` entry
- **Where the gates live:** `src/data/publication-gates.ts` and `scripts/audit-publication.mjs`.
- **Premium content:** `src/data/premiumListings.ts`. The build fails on a key that
  doesn't resolve to a published record.
