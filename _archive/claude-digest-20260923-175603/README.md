# CLAUDE_DIGEST.md, removed from the project root — 20260923-175603

Despite the name, this file was not build notes or a digest of the project. It is the
result block of one old task (adding LeverageAI to two service categories, commit
`12e8498`). Sitting at the root under a `CLAUDE_` name, it read like agent instructions.

The agent entry point is now `AGENTS.md`, and `CLAUDE.md` points to it. Nothing is
deleted — restore by moving the file back to the root.
